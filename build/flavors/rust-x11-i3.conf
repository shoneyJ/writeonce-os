# build/flavors/rust-x11-i3.conf
#
# Rust PID 1 (writeonce-pid1 + writeonce-svc + writeonce-login) + Xorg (X11) +
# i3/i3More, source-built — the original boot path. Corresponds to the `master`
# branch. DECLARED ONLY for now: this flavor's build/skeleton content (Rust crate
# staging, writeonce-installer, writeonce.toml/session-start.sh, kernel 6.12 +
# no Ubuntu base config, …) is migrated into this tree in a follow-on pass — see
# docs/learning/phase-8-wayland-hyprland-quickshell.md.
FLAVOR_INIT=rust
FLAVOR_DISPLAY=x11
FLAVOR_DE=i3more
FLAVOR_PKG=source
FLAVOR_KERNEL=6.12.10
