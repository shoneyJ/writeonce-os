# build/flavors/systemd-x11-i3.conf
#
# systemd init + Xorg (X11) + i3/i3More, source-built. Corresponds to the
# `with-systmed` branch. DECLARED ONLY for now: the framework reads these axes,
# but this flavor's build/skeleton content (X11 stack staging, i3/i3More, startx
# .bash_profile, …) is migrated into this tree in a follow-on pass — see
# docs/learning/phase-8-wayland-hyprland-quickshell.md.
FLAVOR_INIT=systemd
FLAVOR_DISPLAY=x11
FLAVOR_DE=i3more
FLAVOR_PKG=source
FLAVOR_KERNEL=6.18.34
